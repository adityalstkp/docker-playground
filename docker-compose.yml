version: '3'
services:
  play-nginx:
    image: nginx
    volumes:
      - ./nginx/public/:/usr/share/nginx/html/
      - ./nginx/conf.d/:/etc/nginx/conf.d/
    restart: always
    ports:
      - 8000:80
  play-backend:
    build:
      context: ./backend
  play-frontend:
    build:
      context: ./frontend
